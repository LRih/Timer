<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto"
    package="ric.ov.Timer"
    android:versionCode="5"
    android:versionName="1.1.1dev">

    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="23" />

    <application android:label="@string/app_name" android:icon="@drawable/icon" android:theme="@android:style/Theme.DeviceDefault">
        <activity android:name=".Activities.MainActivity" android:label="@string/app_name"  android:configChanges="keyboardHidden|orientation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.CountupTimerActivity" android:label="@string/countup_timer" android:configChanges="keyboardHidden|orientation" />
        <activity android:name=".Activities.CountdownTimerActivity" android:label="@string/countdown_timer" android:configChanges="keyboardHidden|orientation" />
    </application>
</manifest>
